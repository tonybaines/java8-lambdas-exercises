apply plugin: "java"

repositories {
  mavenCentral()
}

dependencies {
  compile([
    'com.javaslang:javaslang:2.0.0-RC3'
  ])
  testCompile([
    'junit:junit:4.12',
    'org.hamcrest:hamcrest-all:1.3',
  ])
}

wrapper {
  gradleVersion = '2.12'
}